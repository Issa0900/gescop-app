{
  "name": "Payroll",
  "type": "object",
  "properties": {
    "payroll_id": {
      "type": "string"
    },
    "employee_id": {
      "type": "string"
    },
    "period": {
      "type": "string"
    },
    "hours": {
      "type": "number"
    },
    "regular_pay": {
      "type": "number"
    },
    "overtime": {
      "type": "number"
    },
    "bonus": {
      "type": "number"
    },
    "employer_cost": {
      "type": "number"
    },
    "total_cost": {
      "type": "number"
    },
    "deductions": {
      "type": "number",
      "description": "Retenues a la source (impots, cotisations salariales)."
    },
    "net_pay": {
      "type": "number",
      "description": "Salaire net verse."
    },
    "payment_date": {
      "type": "string",
      "format": "date",
      "description": "Date du versement."
    },
    "import_id": {
      "type": "string"
    },
    "original_data": {
      "type": "string",
      "description": "Ligne du fichier source telle quelle (JSON), toutes colonnes comprises. Permet de retrouver une valeur non reconnue ou repliee sans reimporter."
    },
    "fingerprint": {
      "type": "string",
      "description": "Empreinte metier de la ligne, utilisee pour detecter les doublons entre imports."
    }
  },
  "required": [
    "employee_id",
    "period"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}